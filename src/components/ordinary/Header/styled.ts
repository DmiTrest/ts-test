import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  header: {
    marginBottom: 24,
    justifyContent: 'center',
    alignItems: 'center',
    height: 50,
    backgroundColor: 'rgb(84,87,92)',
  },
  headerTitle: {
    fontSize: 16,
    fontWeight: '500',
    color: '#fff',
  },
});
